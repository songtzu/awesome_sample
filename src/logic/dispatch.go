package logic

import (
	"awesome/framework"
	"awesome/pb_protocol"

)

func processLeaveRoom(roomData interface{}, req *pb_protocol.UserLogin, user *framework.PlayerImpl) (framework.ProtoMessage, int) {
	return nil,0
}
